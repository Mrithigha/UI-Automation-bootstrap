# Browser: chrome | firefox | edge
browser=chrome

# Set to true to run without opening a browser window
headless=false

# Frontend base URL
base.url=http://localhost:3000

# Selenium implicit wait (seconds)
implicit.wait=10

# Explicit WebDriverWait timeout (seconds)
explicit.wait=15
